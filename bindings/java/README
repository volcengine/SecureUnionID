This is the java bindings (Currently only support Linux).

Run the following command:

`make`
It will use swig to generate the wrap files and then use gcc to compile the libSecureUnionID.so on Linux
or libSecureUnionID.dylib on Mac.

`make build`
It will use java command to build the java classes

`make run`
It will run the demo.
